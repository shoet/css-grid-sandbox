import type { Meta, StoryObj } from "@storybook/react";
import { MainSection } from ".";

export default {
  title: "MainSection",
  component: MainSection,
} as Meta<typeof MainSection>;

type Story = StoryObj<typeof MainSection>;

export const Default: Story = {};
